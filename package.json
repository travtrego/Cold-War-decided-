{
  "name": "cold-war-decided",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node scripts/migrate.mjs",
    "test": "npm run verify && npm run check && npm run test:api",
    "verify": "node scripts/verify-bundle.mjs && node scripts/verify-live-module.mjs",
    "check": "node --check api/agent.js && node --check api/health.js && node --check api/missions.js && node --check api/mission.js && node --check api/evaluate.js && node --check live-access.js && node --check live-mode.js",
    "test:api": "node scripts/test-api.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
