{
  "name": "cold-war-decided",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run verify && npm run check",
    "verify": "node scripts/verify-bundle.mjs",
    "check": "node --check api/agent.js && node --check api/health.js"
  }
}
