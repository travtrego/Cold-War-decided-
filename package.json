{
  "name": "cold-war-decided",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run verify && npm run check && npm run test:api",
    "verify": "node scripts/verify-bundle.mjs && node scripts/verify-live-module.mjs",
    "check": "node --check api/agent.js && node --check api/health.js && node --check live-mode.js",
    "test:api": "node scripts/test-api.mjs"
  }
}
